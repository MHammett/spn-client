## What this changes and why

<!-- If this is a behavior change derived from archive.org's actual behavior
     (a rate limit, an error code, a response field), say what you observed
     and how — see CONTRIBUTING.md. -->

## Checklist

- [ ] `ruff check src tests` / `ruff format --check src tests` pass
- [ ] `mypy src` passes
- [ ] `pytest --cov=src/spn_client` passes and coverage hasn't dropped
- [ ] `CHANGELOG.md` updated under `[Unreleased]` if this is user-visible
